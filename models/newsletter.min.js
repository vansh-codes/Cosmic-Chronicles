const mongoose=require("mongoose");const Schema=mongoose.Schema;const newsLetterSchema=new Schema({email:{type:String,unique:true,required:true}});module.exports=mongoose.model("newsletter",newsLetterSchema);